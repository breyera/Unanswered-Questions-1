<main class="p-7 pt-20 bg-black bg-opacity-80 flex flex-col justify-center w-screen min-h-screen">
    <!--  qotd card  -->
    <div class="flex flex-col items-center">
        <h1>Daily Question</h1>
        <h2>{{daily_question.question}}</h2>


    </div>


    <!--  commment section  -->
    <div class='m-14 p-7 bg-black bg-opacity-80 flex flex-col justify-center items-center'>
        <h2 class='flex-1 items-center mb-10'>Comments</h2>
        <!--  each comments as comment card  -->
        {{#each daily_question.comments as |comment|}}
        <div class='flex flex-row my-4'>
            <h3 class='pr-3'>{{comment.user.user_name}}</h3>
            <p>{{comment.comment}}</p>

        </div>
        {{/each}}


        <!--  submit comment   -->
        {{#if loggedIn}}
        <!--comment input -->
        <div class='flex flex-1 flex-col justify-center items-center mt-5'>
            <h4>Join the discussion</h4>

            <input id="comment" type="text"><button class=' min-w-max' id="submit" data-userid={{currentUserId}}
                data-dq={{daily_question.id}}>Submit</button>

        </div>
        {{else}}
        <!-- link to the login page -->
        <div class="flex flex-1 flex-col justify-center items-center">
            <h4 class='flex-1'>Log in to join the discussion.</h4>

            <a class='flex-1' href="/login">
                <button class=' min-w-max' type="submit">Log in</button>
            </a>

        </div>
        {{/if}}

    </div>
</main>
<script src="/js/qotd.js"></script>